[project]
name = "pysvsim"
version = "0.1.0"
description = "Pure Python SystemVerilog simulator for educational digital logic modules and CPUs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pillow>=11.2.1",
]
